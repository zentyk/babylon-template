version: "3"

services:
    Template:
        container_name: "BabylonTemplate"
        image: nginx:1.14.2-alpine
        restart: always
        ports:
            - "80:80"
        volumes:
            - ./dist:/usr/share/nginx/html:ro